# -*- coding: utf-8 -*-
"""Dash callbacks, registered on import; one module per app concern."""

from app_src.callbacks import (
    clientside,
    loading,
    navigation,
    prediction,
    saving,
    video,
)
