{
  "name": "sleep-scoring-clientside-tests",
  "private": true,
  "description": "Jest unit tests for the Dash clientside callbacks in app_src/assets/clientsideCallbacks.js",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^30.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFiles": [
      "<rootDir>/setup.js"
    ]
  }
}
